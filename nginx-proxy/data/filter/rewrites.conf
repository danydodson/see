
# redirect to http://nance.com
# if ($host !~* ^\(.*)$) {
#     return 301 $scheme://$host$request_uri;
# }?

# removes trailing index
# if ($request_uri ~* index/?$) {
#     rewrite ^/(.*)/index/?$ /$1 permanent;
# }

# removes trailing slashes
# if (!-d $request_filename) {
#     rewrite ^/(.+)/$ /$1 permanent;
# }

